[project]
name = "quiz-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=23.0.0",
    "pillow>=12.3.0",
    "python-dotenv>=1.0.1",
    "requests>=2.34.2",
]
